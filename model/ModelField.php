<?php
	require_once 'Model.php';

	class ModelField extends Model {
		/*
		private $type;
		private $required;
		private $order;
		private $default_value;
		private $length;
		private $values;
		private $section_id;
		private $nom;
		private $option;

		public function get($attribut) {
		   return $this->$attribut;
		}

		public function set($attribut, $valeur) {
		   $this->$attribut = $valeur;
		}

		public function __construct($nom = NULL, $required = NULL, $order = NULL, $type = NULL, $default_value = NULL, $values = NULL, $section_id = NULL, $option = NULL, $length = NULL) {
		  $this->nom = $nom;
		  $this->required = $required;
		  $this->order = $order;
		  $this->type = $type;
		  $this->default_value = $default_value;
		  $this->values = $values;
		  $this->section_id = $section_id;
		  $this->option = $option;
		  $this->length = $length;
		} */

		public static $object = "Field";


	}
